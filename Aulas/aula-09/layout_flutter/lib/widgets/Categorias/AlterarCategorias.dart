import 'package:flutter/material.dart';

class AlterarCategorias extends StatelessWidget{
  const AlterarCategorias({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Alterar ')),
    );
  }
}